= render "admin/dashboard/tabs"

= form_for [:admin, @site_admin] do |f|
  = error_messages_for :site_admin
  %fieldset
    %legend New Site Admin
    = render "form", :f => f
    %ol= render "admin/users/password_fields", :f => f
    %ul
      %li= f.submit
